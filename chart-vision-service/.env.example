# Required for real AI chart readings. Without this set, the server runs in
# "mock" mode and returns a clearly-labeled placeholder response instead.
ANTHROPIC_API_KEY=

# Optional, defaults to 4000
PORT=4000
