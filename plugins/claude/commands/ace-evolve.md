---
description: "Moved — use /ace:evolve instead of global /ace-evolve"
---
This command was moved into the **ace** plugin namespace.

Use **/ace:evolve** (or Skill("ace-evolve")) instead of the legacy global **/ace-evolve** command.
