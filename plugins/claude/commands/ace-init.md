---
description: "Moved — use /ace:init instead of global /ace-init"
---
This command was moved into the **ace** plugin namespace.

Use **/ace:init** (or Skill("ace-init")) instead of the legacy global **/ace-init** command.
